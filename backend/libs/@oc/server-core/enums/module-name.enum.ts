/**
 * Enum containing all available module names for type safety
 * Use these enum values in services instead of string literals
 */
export enum ModuleName {
    // User Management
    USER = "User",

    // Authentication
    AUTH = "Auth",

    // Product Management
    PRODUCT = "Product",
    CATEGORY = "Category",
    REVIEW = "Review",

    // Analytics
    ANALYTICS = "Analytics",

    // Import
    IMPORT = "Import"
}
